version: '3.4'
services:
  jamespc:
    build: ./jamespc
    cap_add:
      - LINUX_IMMUTABLE

  randalpc:
    build: ./randalpc

  xssbot:
    build: ./xssbot

  startingpoint:
    build: ./startingpoint

  tompc:
    build: ./tompc

  jwtbot:
    build: ./tompc/discordbot

  finale:
    build: ./finale
    ports:
      - 2222:22
